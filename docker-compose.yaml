version: "3.11"
services:
  dashboard:
    build:
      context: ./
    container_name: "medic"
    hostname: "medic"
    restart: always
    ports:
      - "5000:5000"
    networks:
      - network

networks:
    network:
        driver: bridge